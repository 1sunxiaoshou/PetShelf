# 静态 Demo 验证记录

验证日期：2026-09-03。

## 已通过

- `npm test`：7 项自动化测试全部通过，覆盖 V1/V2 图集、全部方向帧、搜索排序、收藏异常数据、12 份图像摘要和 ZIP 内容，以及界面文案回归检查。
- `npm run build`：成功，前端 JS 约 233 KB（gzip 74 KB）。
- `git diff --check`：通过。
- 开发页与构建后的静态预览：12 张素材全部加载，实际自然尺寸符合各自版本。
- V1 详情：1536×1872 背景定位正确。
- V2 详情：1536×2288，337.5° 对应最后一行最后一格（x=-1344，y=-2080）。
- 播放/暂停、方向滑杆、名称排序、无搜索结果状态均通过 UI 操作验证。
- 收藏筛选只显示已收藏对象；刷新后收藏仍在。
- 本地 V2 文件夹可预览，文件不发送到服务端。
- 缺失 spritesheet 的测试文件夹显示错误，不允许播放。
- 构建版 ZIP 下载已通过浏览器下载事件验证；包内原始图集、manifest、许可已由自动测试检查。
- Esc 关闭弹窗、返回页面；手机断点 390×844 下首页和详情无横向溢出。
- 页面控制台未捕获 error/warn。

## 截图

- `screenshots/01-gallery.png`：桌面首页。
- `screenshots/02-v2-directions.png`：宠物详情（沿用文件名）。
- `screenshots/03-local-preview.png`：本地文件夹预览。
- `screenshots/04-mobile-detail.png`：手机详情。

## 边界

历史后端和全栈计划只保留作参考，不适用于生产。旧实施计划包含数据库凭据，公开仓库前应先轮换并处理 Git 历史。

## 文案清理

2026-09-03 更新：移除产品页面中的 Demo 定位、技术参数、版本徽标、动作数量和实现解释；来源与许可收为详情小链接。界面角色简介与原始 manifest 分离，下载包继续保留原始文件。上述四张截图均重新拍摄，README 保留简短介绍、运行命令与预览图。

没有验证当前 Codex 客户端是否安装每个素材；本项目只保证其网页 Demo 中的预览和原始包下载。没有连接历史数据库、启动历史后端、轮换账户凭据或部署到公网。社区素材许可和来源随包保留，不代表对第三方角色权利的独立保证。
